---
status: active
created: 2026-08-27
updated: 2026-08-29
attempts: 2
attempt_cap: 10
---

## Objective

当前 APP（io.github.lzyuuu.ailivesaver）与参考 fancy-ai V4.51（com.mrj.fancyai.github，
V4.51-github-release.apk）全量对照：复刻参考的全部功能，且**除桌面外**所有页面 UI
一比一对齐参考 V4.51 的样式与设计。分三阶段验收：

- **阶段①**：除桌面外全部页面 UI 一比一 + 现有页面内功能补齐（聊天抽屉死开关、
  Imaging 本地模型下载、设置清理页、常规页合并等审计缺口）
- **阶段②**：本地推理链路（#56：模型下载/导入、llama.cpp/LiteRT 引擎、专家采样
  实接、Benchmark、CPU/GPU/NPU 后端切换）
- **阶段③**：占位子应用逐个落地（Games Hub 六游戏、Phone、Lorebook、Root Creator、
  Benchmark、Root Producer、HD Upscaler、Groups 大厅）

## Success criteria

1. 以 `walkthrough/2026-08-25-replica-audit/coverage-checklist.md` 为基线的全新全量
   走查 checklist：参考有的每一行状态 DONE 且**无"缺失确认"标注**（桌面行除外，
   桌面只验功能对齐不验 UI 一比一）
2. 参考有的每个页面都有双端（我方 vs 参考）同屏截图对照，归档于 `walkthrough/`，
   逐页通过样式核对
3. `./gradlew :app:testDebugUnitTest` 全绿；新增功能带测试
4. 三阶段各有验收记录（tracker issue + 证据）

## Verification

1. 全量走查：mobile-mcp/adb 双端逐页遍历，生成新 coverage-checklist（沿用 08-25
   格式），脚本校验截图文件存在
2. UI 对照：逐页双端截图并排比对（协议同 #66 双端走查）
3. `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
   ./gradlew :app:testDebugUnitTest` 退出 0
4. 每阶段结束在 tracker 开验收 issue 附证据

## Boundaries

- 允许：`app/src/**`、测试、`walkthrough/` 证据、tracker issue、CONTEXT.md/ADR
- 桌面（FANCY OS 主屏）：视觉风格保留现状（虚拟手机/单页/金色暗调），不重构；
  功能差距照补，UI 仅兼容性微调
- 禁止：编辑 `.env`；未经询问 `git push`；改动 `.mimosa/` 与钩子状态；提交无关
  脏工作区（SocialScreens.kt 删除等在研改动）
- 参考侧只读：仅安装/运行/截图对照
- Pro/付费墙：沿用仓库既定原则——入口开放、无 Pro 墙
- Mimosa 拦截时：先报告再处置，不擅自移动/删除项目文件

## Stop conditions

- 参考功能依赖无法获得的外部资产（Gemma 模型源不可达、付费 API），模拟器/真机
  无法验证 → 停下汇报等决策
- attempts ≥ 10
- 高风险操作需求（覆盖用户在研改动等）→ 停下确认
- 阶段②本地推理在当前硬件上验证不可行（spike 结论驱动）→ 停下汇报

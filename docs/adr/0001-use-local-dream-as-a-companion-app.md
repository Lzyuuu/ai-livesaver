# 将 Local Dream 作为伴生应用

主应用保持独立 APK，通过 Local Dream 在 `127.0.0.1:8081` 提供的本机 HTTP API 请求图片生成，并消费 SSE 生成进度；用户需另行安装 Local Dream、选择模型并完成加载。相比把其源码和 QNN 运行时嵌入主应用，这一方案接受额外的安装与就绪引导，换取更清晰的应用边界、独立升级能力，并避免让主应用直接受 Local Dream 的 CC BY-NC 4.0 源码许可证约束。

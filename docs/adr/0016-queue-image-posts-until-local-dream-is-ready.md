# 配图等待 Local Dream 就绪后再发布

后台世界推进可以在 Local Dream 未运行时继续发布纯文字社交流内容；明确需要图片的事件只保存配图意图，等待 Local Dream 就绪并成功生成图片后再正式发布。创作队列提供“一键打开 Local Dream”，但后台任务不得擅自跳转应用；用户在 Local Dream 加载模型并返回后，主应用自动检测并继续队列。失败任务保留提示词和参数以供重试。主应用不展示假图片或空卡片，也不自动回退云端生图；已生成图片允许用户手动请求直接重绘或修改提示词后重绘。

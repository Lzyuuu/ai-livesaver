# 本地世界可完整备份但不包含凭据

首版提供可重新导入的单文件备份：完整备份覆盖角色、聊天、帖子、关系、长期记忆、设置和生成图片，轻量备份包含相同结构化数据但不含媒体；API Key 等凭据均不随备份导出。世界备份恢复只支持完整替换，不合并两个世界；导入前完整校验并为当前世界创建临时安全备份，校验成功后原子替换，失败时保持当前世界不变。备份文件首版不加密，导出时必须明确提示其中包含私密内容。应用不实现账号或云同步，以较小的实现范围保护不可替代的本地世界数据，同时避免备份文件泄露云端 Provider 凭据。

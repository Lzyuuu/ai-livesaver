# 对 AI 内容生成新版本而非静默修改

用户自己的帖子和评论可以直接编辑或删除。AI 角色发布的内容允许隐藏、删除或请求角色重写，但不得由用户直接改字后仍显示为角色原话；重写产生可查看和恢复的新版本，社交流只展示当前版本。删除内容时同时移除由它产生但尚未固定的长期记忆，已固定记忆需要单独确认。NPC 内容可以直接删除而不保留版本。Provider、模型和生成时间只在内容详情或运行记录中显示。

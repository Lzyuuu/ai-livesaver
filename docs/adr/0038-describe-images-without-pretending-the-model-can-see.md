# 使用媒体描述而不假装模型能够看图

每张用户图片可以保存一段默认不公开展示的媒体描述，供 AI 角色理解并回应。只有来源未知且没有现成描述的用户上传图片才触发视觉理解：优先使用独立的视觉推理配置，未配置时尝试通用推理配置自身的多模态能力，并允许用户修改结果。两者均不支持视觉时由用户填写或留空，留空后角色不得假装看见图片细节。由主应用调用 Local Dream 生成的图片直接使用已知提示词形成媒体描述；用户在 Local Dream 中手动生成的图片可以通过图片导入与参数粘贴带回提示词。仅导入图片但没有参数时仍需用户填写，因为主应用不能假设图片文件包含可读取的提示词。

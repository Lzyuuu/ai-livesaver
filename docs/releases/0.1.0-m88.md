# AI Livesaver 0.1.0-m88

- 世界恢复在替换数据前持久化旧数据库、媒体存在状态和世界设置，并只在快照完成后进入可恢复状态。
- 如果应用在恢复提交前被系统终止，下次启动会先恢复旧世界，再打开数据库；不会把 `rollback-*` 误当作普通缓存清理。
- 恢复完全提交后保留新世界并清理安全副本。

本版本仍是 GitHub Releases 测试版；真实大体积媒体备份与 OEM 强制终止仍需在 iQOO 15 Ultra 上验收。

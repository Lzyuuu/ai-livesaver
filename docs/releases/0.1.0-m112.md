# AI Livesaver 0.1.0-m112

- **Fancy Store 商店形态**（v4.50 基座，切片 T1–T6，issue #39–#44）：系统桌面保留，商店作为 Dock 常驻入口；目录 17 产品（14 app + 3 package），三档可用性（可获取 6 / 即将开放 2 / 即将推出 9）；`app_install` 表持久化安装状态与「添加到首页」（DB v24→v25 无损迁移）；App 安装 = 模拟下载进度 + 本地解锁；未完成能力诚实呈现，无 Pro 付费墙。
- **入口门控**：`openDesktopApp` 统一安装态校验，未安装的 App 从 Hub、Dock、深链（`OPEN_DESKTOP_APP_EXTRA`）、通知/世界事件均降级到商店引导；已安装 App 功能与迁移前一致。
- **Dock**：固定 5 槽（Messenger / Imaging / Gallery / 商店 / Settings），「添加到首页」的 App 追加进 Dock，商店槽不被挤占，桌面与商店状态双向一致。
- **备份/擦除语义**：`WorldBackup.export`/`restore` 闭环保留 `app_install`；`eraseAll` 同时清除安装状态。
- **应用图标**：更换为用户提供的兔子像素画，覆盖各密度 mipmap 与自适应图标前景。

验证（emulator-5554 / API 28）：`testDebugUnitTest`（`FancyStoreCatalogTest` 5/0、`DesktopRoutesTest` 7/0）、商店相关 instrumentation 24/0（迁移 7 + 商店流 5 + 门控/Dock 7 + 备份恢复 4 + 擦除 1）、`assembleRelease` 通过；启动检查两次无 FATAL/ANR；对照截图 `screenshot/comparison/2026-08-13-v450-store/`。

偏差：双 API 回归仅 API 28（API 36 AVD 未启动）；全量套件中外部依赖失败（HF 502 / Provider 超时 / Local Dream 未运行）与商店改动无关。

本版本为 GitHub Releases 测试版。APK 使用 Android Debug 测试证书签名，可覆盖升级 m111；请勿将该测试证书用于正式生产发布。

APK：`app-release.apk`

APK SHA-256：`e50a647c6d9b8fbb60cfcf5b855fb49bb6478415ed8aedd5f958c0146c2293dd`

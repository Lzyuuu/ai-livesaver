# AI Livesaver 0.1.0-m29

这次里程碑把模拟器验收固化为 Android UI 烟测。

- 新增 Compose instrumentation smoke test，验证五个主入口可见。
- 验证 `Me → 关于与更新` 可以进入更新页面并显示检查按钮。
- 测试使用真实 `MainActivity` 和 Compose 语义树，不替换页面实现。

## 验证

- `./gradlew testDebugUnitTest lintDebug assembleRelease`
- `./gradlew connectedDebugAndroidTest`（API 36 与 API 28 模拟器）
- Release APK 使用与前序里程碑相同的测试签名构建。

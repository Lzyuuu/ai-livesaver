# AI Livesaver 0.1.0-m18

这次里程碑让用户身份变化进入世界历史，但不泄露身份细节。

- 修改昵称、称呼偏好或自我描述时，会追加 `identity_change` 世界事件。
- 事件只记录“用户更新了世界身份”的公开摘要，不把具体自述复制到社交流或通知。
- 既有聊天、记忆和关系不会被追溯改写；当前身份继续用于未来场景。
- 身份未变化时不会产生空事件。

## 验证

- `./gradlew testDebugUnitTest lintDebug`
- Release APK 使用与前序里程碑相同的测试签名构建。

# AI Livesaver 0.1.0-m24

这次里程碑补齐角色配图的可选全局风格。

- Me → 世界设置中可以填写全局配图风格。
- Local Dream 提示词顺序固定为角色外观、服装、全局风格、当前场景。
- 全局风格只是一段本地提示词，不绑定模型或 LoRA。
- 设置会随世界设置备份保存；留空时保持原有行为。

## 验证

- `./gradlew testDebugUnitTest lintDebug assembleRelease`
- Release APK 使用与前序里程碑相同的测试签名构建。

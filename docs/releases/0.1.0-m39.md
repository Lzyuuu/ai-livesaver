# AI Livesaver 0.1.0-m39

## 本里程碑

- `Me` 新增外观选择：跟随系统、浅色、深色；选择会保存并在下次启动继续生效。
- Android 12 以上仍使用 Material 3 动态色，固定主题只改变明暗模式。
- 聊天列表滚动遵循系统动画缩放设置；启用“移除动画”时直接定位，不再播放滚动动画。
- 新增主题入口 UI 烟测和动画偏好单元测试。

## 验证

- `testDebugUnitTest`
- `lintDebug`
- `assembleRelease`
- `connectedDebugAndroidTest`：API 28、API 36 各 4 项通过

此版本仍是 GitHub Releases 测试版，真实 iQOO 15 Ultra 与 Local Dream 联调需要用户侧载后验收。

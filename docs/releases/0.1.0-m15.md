# AI Livesaver 0.1.0-m15

这次里程碑让核心关系能够停滞、靠近，也能真实地出现分歧与修复。

- 直接对话中的有限关键词信号会触发叙事关系变化：`关系出现裂痕`、`有些疏远`、`重新靠近`。
- 关系变化仍然保存为独立事件，保留来源消息和生成时间，不显示好感度数字。
- 固定关系继续阻止自动推进；用户修正和历史记录不受影响。
- 关键词只作为低成本边界信号，不把普通社交闲聊自动升级为关系变化。

## 验证

- `./gradlew testDebugUnitTest lintDebug`
- Release APK 使用与前序里程碑相同的测试签名构建。

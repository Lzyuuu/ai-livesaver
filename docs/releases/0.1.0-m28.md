# AI Livesaver 0.1.0-m28

这次里程碑收紧用户头像的本地生命周期。

- 替换或移除头像后，旧的应用管理头像文件会被清理。
- 只删除 `files/media` 下带有 `user-avatar-` 前缀的旧文件，不触碰导入图片、生成图片或外部路径。
- 身份历史事件仍然保留，头像不会被上传到 Provider。

## 验证

- `./gradlew testDebugUnitTest lintDebug assembleRelease`
- Release APK 使用与前序里程碑相同的测试签名构建。

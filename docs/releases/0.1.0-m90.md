# AI Livesaver 0.1.0-m90

- 世界备份不再在开放写入窗口中直接复制 `world.db`。
- 导出会先完成 WAL checkpoint，再在数据库写事务锁内复制一致快照，避免后台世界事件与备份文件跨页竞争。
- Android 回归会打开复制出的独立 SQLite 文件并验证刚写入的世界成员可读。

本版本仍是 GitHub Releases 测试版；大体积世界的导出耗时与后台竞争仍需在 iQOO 15 Ultra 上验收。

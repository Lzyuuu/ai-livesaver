# AI Livesaver 0.1.0-m26

这次里程碑修复备份操作后的数据库生命周期。

- 导出前先 checkpoint SQLite WAL，但不会关闭正在使用的世界数据库；导出完成后可以继续浏览和编辑。
- 恢复失败后自动重建 Activity，避免恢复流程关闭数据库后留下不可用页面。
- 备份仍然先完整校验，失败时保留当前世界。

## 验证

- `./gradlew testDebugUnitTest lintDebug assembleRelease`
- Release APK 使用与前序里程碑相同的测试签名构建。

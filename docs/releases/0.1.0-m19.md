# AI Livesaver 0.1.0-m19

这次里程碑把后台世界补演的时间边界抽成可验证规则。

- 用户离开时间达到当前世界活跃度间隔，才允许触发一次返回补演。
- 最近已有后台事件时不会重复补演，避免把世界填满。
- 系统时钟回拨或时间戳异常时不会误触发补演。
- 规则继续使用同一条 `WorldEngine.generate(reconstructed = true)` 持久化路径，保留补演事件来源。

## 验证

- `./gradlew testDebugUnitTest lintDebug`
- Release APK 使用与前序里程碑相同的测试签名构建。

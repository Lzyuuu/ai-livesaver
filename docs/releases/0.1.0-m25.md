# AI Livesaver 0.1.0-m25

这次里程碑补齐应用内更新的防丢数据提示。

- `Me → 关于与更新` 发现新版本后，下载前会提醒先导出世界备份。
- 提示框可以直接跳到 `Me → 备份与恢复`，也允许用户确认后继续交给浏览器下载。
- 下载和安装仍由 GitHub、浏览器与 Android 系统负责，不做静默安装。

## 验证

- `./gradlew testDebugUnitTest lintDebug assembleRelease`
- Release APK 使用与前序里程碑相同的测试签名构建。

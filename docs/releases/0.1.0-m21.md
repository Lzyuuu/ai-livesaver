# AI Livesaver 0.1.0-m21

这次里程碑补齐了用户在世界中的视觉身份。

- Me → 用户身份支持使用系统图片选择器设置或移除头像。
- 头像复制到应用私有目录，不申请照片库或存储权限。
- World、Me、Moments、Commons 的用户身份展示会使用头像；没有头像时继续显示首字母。
- 数据库从 v18 迁移到 v19，既有身份资料保持不变。

## 验证

- `./gradlew testDebugUnitTest lintDebug assembleRelease`
- Release APK 使用与前序里程碑相同的测试签名构建。

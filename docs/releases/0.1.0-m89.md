# AI Livesaver 0.1.0-m89

- 轻量备份明确清空用户头像路径，避免无媒体备份恢复后保留失效的旧设备绝对路径。
- 完整备份恢复会按归档内实际存在的头像文件重写新设备路径；文件缺失时回退到文字头像。
- 帖子图片、生图版本和用户头像现在遵循一致的“文件存在才恢复引用”规则。

本版本仍是 GitHub Releases 测试版；跨设备完整备份恢复仍需在 iQOO 15 Ultra 上验收。
